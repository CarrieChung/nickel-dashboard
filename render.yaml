services:
  - type: web
    name: nickel-dashboard
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn nickel.app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 120"
    autoDeploy: true
services:
  - type: web
    name: nickel-dashboard
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn nickel.app:app --bind 0.0.0.0:$PORT --workers 1 --threads 8 --timeout 60 --graceful-timeout 20 --max-requests 350 --max-requests-jitter 50"
    autoDeploy: true